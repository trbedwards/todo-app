{
	"name": "todo-app-dev",
	"features": {
		"ghcr.io/devcontainers/features/miniforge:1": {
			"version": "latest"
		}
	},
	"forwardPorts": [8000],
	"postCreateCommand": "sudo apt-get update -y && sudo apt-get install -y xvfb libsdl2-2.0-0 libsdl2-image-2.0-0 libsdl2-ttf-2.0-0 libmtdev1 xclip xsel fonts-dejavu-core && bash -lc 'source $HOME/miniforge3/etc/profile.d/conda.sh && (conda env create -f environment.yml -n todo-app || conda env update -n todo-app -f environment.yml --prune) && conda run -n todo-app pip install --no-input uvicorn[standard] watchfiles httpx websockets pillow'",
	"customizations": {
		"vscode": {
			"extensions": [
				"ms-python.python"
			]
		}
	}
}